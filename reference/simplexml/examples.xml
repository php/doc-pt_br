<?xml version="1.0" encoding="iso-8859-1"?>
<!-- EN-Revision: n/a Maintainer: rafa Status: ready --><!-- CREDITS: rafael -->

<chapter xml:id="simplexml.examples" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 &reftitle.examples;
 <section xml:id="simplexml.examples-basic">
  <para>
   Muitos exemplos vistos aqui requerem uma string XML. Ao invés de ficar repetindo
   esta string em cada exemplo, vamos colocá-la em um arquivo que iremos incluir
   em cada exemplo. Este arquivo é mostrado no exemplo a seguir.
   Porém, você pode criar um document XML e lê-lo utilizando
   <function>simplexml_load_file</function>.
  </para>
  <para>
   <example>
    <title>Arquivo example.php com uma string XML</title>
    <programlisting role="php" xml:id="simplexml.examples.movie">
<![CDATA[
<?php
$xmlstr = <<<XML
<?xml version='1.0' standalone='yes'?>
<movies>
 <movie>
  <title>PHP: Behind the Parser</title>
  <characters>
   <character>
    <name>Ms. Coder</name>
    <actor>Onlivia Actora</actor>
   </character>
   <character>
    <name>Mr. Coder</name>
    <actor>El Act&#211;r</actor>
   </character>
  </characters>
  <plot>
   So, this language. It's like, a programming language. Or is it a
   scripting language? All is revealed in this thrilling horror spoof
   of a documentary.
  </plot>
  <great-lines>
   <line>PHP solves all my web problems</line>
  </great-lines>
  <rating type="thumbs">7</rating>
  <rating type="stars">5</rating>
 </movie>
</movies>
XML;
?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   A simplicidade do SimpleXML aparece mais claramente quando extraímos uma
   string ou um número de um documento XML básico.
   <example>
    <title>Getting <literal>&lt;plot&gt;</literal></title>
    <programlisting role="php"><![CDATA[
<?php
include 'example.php';

$xml = new SimpleXMLElement($xmlstr);

echo $xml->movie[0]->plot; // "So this language. It's like..."
?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   A tarefa de acessar elementos de dentro de um documento XML que contêm
   caracteres não permitidos pela a convenção de nomes do PHP (por exemplo,
   o hífen) pode ser feita encapsulando o elemento com chaves e apóstrofos.
   <example>
    <title>Obtendo <literal>&lt;line&gt;</literal></title>
    <programlisting role="php">
<![CDATA[
<?php
include 'example.php';

$xml = new SimpleXMLElement($xmlstr);

echo $xml->movie->{'great-lines'}->line; // "PHP solves all my web problems"
?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   <example>
    <title>Acessando elementos não-únicos no SimpleXML</title>
    <simpara>
     Caso existam nós filhos com nomes iguais dentro de um mesmo nó pai, aplica-se
     a iteração normal.
    </simpara>
    <programlisting role="php">
<![CDATA[
<?php
include 'example.php';

$xml = new SimpleXMLElement($xmlstr);

/* For each <movie> node, we echo a separate <plot>. */
foreach ($xml->movie as $movie) {
   echo $movie->plot, '<br />';
}

?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   <example>
    <title>Utilizando atributos</title>
    <simpara>
     Até agora, só cobrimos o trabalho de ler o nome dos elementos e os seus valores.
     SimpleXML pode também acessar o atributo dos elementos. Acessar os atributos de
     um elemento é tão simples quando acessar os atributos de uma <type>array</type>.
    </simpara>
    <programlisting role="php">
<![CDATA[
<?php
include 'example.php';

$xml = new SimpleXMLElement($xmlstr);

/* Access the <rating> nodes of the first movie.
 * Output the rating scale, too. */
foreach ($xml->movie[0]->rating as $rating) {
    switch((string) $rating['type']) { // Get attributes as element indices
    case 'thumbs':
        echo $rating, ' thumbs up';
        break;
    case 'stars':
        echo $rating, ' stars';
        break;
    }
}
?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   <example>
    <title>Comparando elementos e atributos com texto</title>
    <simpara>
     Para comparar um elemento ou atributo com uma string ou passar para uma
     função que requer uma string, você deve converter para uma string utilizando
     <literal>(string)</literal>. De outra forma, o PHP irá tratar este elemento
     como um objeto.
    </simpara>
    <programlisting role="php">
<![CDATA[
<?php
include 'example.php';

$xml = new SimpleXMLElement($xmlstr);

if ((string) $xml->movie->title == 'PHP: Behind the Parser') {
    print 'My favorite movie.';
}

htmlentities((string) $xml->movie->title);
?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   <example>
    <title>Utilizando XPath</title>
    <simpara>
     SimpleXML possui suporte a <acronym>XPath</acronym> embutido.
     Para encontrar todos os elementos <literal>&lt;character&gt;</literal>:
    </simpara>
    <programlisting role="php">
<![CDATA[
<?php
include 'example.php';
$xml = new SimpleXMLElement($xmlstr);

foreach ($xml->xpath('//character') as $character) {
    echo $character->name, 'played by ', $character->actor, '<br />';
}
?>
]]>
    </programlisting>
    <simpara>
     '<literal>//</literal>' serve como uma wildcard. Para especificar um caminho
     absoluto, basta omitir uma das barras.
    </simpara>
   </example>
  </para>
  <para>
   <example>
    <title>Atribuindo valores</title>
    <simpara>
     Dados no SimpleXML não precisam ser constantes, necessariamente. O objeto permite
     a manipulação de todos os seus elementos.
    </simpara>
    <programlisting role="php">
<![CDATA[
<?php
include 'example.php';
$xml = new SimpleXMLElement($xmlstr);

$xml->movie[0]->characters->character[0]->name = 'Miss Coder';

echo $xml->asXML();
?>
]]>
    </programlisting>
    <simpara>
     O código acima irá mostrar um novo documento XML, como o original,
     exceto que o novo irá trocar Ms. Coder por Miss Coder.
    </simpara>
   </example>
  </para>
  <para>
   <example>
    <title>Adicionando elementos e atributos</title>
    <simpara>
     Desde o PHP 5.1.3, SimpleXML possui a habilidade de adicionar
     atributos e nós filhos facilmente.
    </simpara>
    <programlisting role="php">
<![CDATA[
<?php
include 'example.php';
$xml = new SimpleXMLElement($xmlstr);

$character = $xml->movie[0]->characters->addChild('character');
$character->addChild('name', 'Mr. Parser');
$character->addChild('actor', 'John Doe');

$rating = $xml->movie[0]->addChild('rating', 'PG');
$rating->addAttribute('type', 'mpaa');

echo $xml->asXML();
?>
]]>
    </programlisting>
    <simpara>
     O código acima irá mostrar um documento XML baseado no original, mas contendo
     um novo personagem e um novo rating.
    </simpara>
   </example>
  </para>
  <para>
   <example>
    <title>Interoperabilidade DOM</title>
    <simpara>
     PHP possui um mecanismo para converter nós XML entre os formatos SimpleXML e DOM.
     Este exemplo mostra como mudar um elemento DOM para SimpleXML.
    </simpara>
    <programlisting role="php">
<![CDATA[
<?php
$dom = new domDocument;
$dom->loadXML('<books><book><title>blah</title></book></books>');
if (!$dom) {
     echo 'Error while parsing the document';
     exit;
}

$s = simplexml_import_dom($dom);

echo $s->book[0]->title;
?>
]]>
    </programlisting>
   </example>
  </para>
 </section>
</chapter>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->


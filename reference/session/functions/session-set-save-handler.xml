<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.1 $ -->
<!-- splitted from ./en/functions/session.xml, last change in rev 1.23 -->
  <refentry id="function.session-set-save-handler">
   <refnamediv>
    <refname>session_set_save_handler</refname>
    <refpurpose>
     Define a sequ&ecirc;ncia de fun&ccedil;&otilde;es de armazenamento 
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>Descri&ccedil;&atilde;o</title>
     <methodsynopsis>
      <type>bool</type><methodname>session_set_save_handler</methodname>
      <methodparam><type>string</type><parameter>open</parameter></methodparam><methodparam><type>string</type><parameter>close</parameter></methodparam><methodparam><type>string</type><parameter>read</parameter></methodparam><methodparam><type>string</type><parameter>write</parameter></methodparam><methodparam><type>string</type><parameter>destroy</parameter></methodparam><methodparam><type>string</type><parameter>gc</parameter></methodparam>
     </methodsynopsis>
    <para>
     <function>session_set_save_handler</function> define a sequ&ecirc;ncia 
     de fun&ccedil;&otilde;es de armazenamento  que &eacute; usada para guardar e devolver
     dados associados &agrave; sess&atilde;o. Esta &eacute; mais usual quando um
     quando um m&eacute;todo de armazenamento, a n&atilde;o ser que aquele oferecido por 
     sess&otilde;es do PHP seja prefer&iacute;vel.  i.e. Guardar dados de sess&atilde;o em um
     banco de dados local.
     &return.success;
    </para>
    <note>
     <para>
      Voc&ecirc; deve definir as op&ccedil;&otilde;es de configura&ccedil;&atilde;o
      <parameter>session.save_handler</parameter> para
      <parameter>user</parameter> em seu arquivo &php.ini; para
      <function>session_set_save_handler</function> funcionar.
     </para>
    </note>
    <note>
     <para>
      A fun&ccedil;&atilde;o "write" handler n&atilde;o &eacute; executada at&eacute; depois que o fluxo de sa&iacute;da
      esteja fechado. Assim, a sa&iacute;da de instru&ccedil;&otilde;es debugging na fun&ccedil;&atilde;o
      "write" handler poder&aacute; nunca ser vista pelo navegador. Se a sa&iacute;da 
      debugging &eacute; necess&aacute;ria, ao inv&eacute;s disso &eacute; sugerido que a sa&iacute;da debug 
      seja escrita para um arquivo.
     </para>
    </note>
    <note>
     <para>
      A fun&ccedil;&atilde;o "write" handler n&atilde;o &eacute; executada se a sess&atilde;o n&atilde;o cont&eacute;m dados;
      isto se aplica sempre se vari&aacute;veis de sess&atilde;o vazias s&atilde;o registradas. Esta
      difere &agrave; sess&atilde;o padr&atilde;o de save handler baseado em arquivos, que cria
      arquivos de sess&atilde;o vazios.
     </para>
    </note>
    <para>
     O seguinte exemplo oferece um aramzenamento de sess&atilde;o baseado em
      arquivos similar a sess&otilde;es de PHP padr&otilde;es save handler
     <parameter>files</parameter>.  Este exemplo poderia facilmente ser
     extendido para outras bases de dados usando seu gerente de banco de dados favorito suportado pelo PHP.
    </para>
    <para>
     A fun&ccedil;&atilde;o "Read" deve retornar um valor string sempre que fizer o save handler
      trabalhar como o esperado. Retorna uma string vazia se n&atilde;o existe dados para
     ler. Retorna valores de outros handlers que estejam convertidos para express&otilde;es booleanas. TRUE em sucesso, FALSE em falha.
    </para>
    <para>
     <example>
      <title>
       <function>session_set_save_handler</function> exemplo
      </title>
      <programlisting role="php">
<![CDATA[
<?php
function open ($save_path, $session_name) {
  global $sess_save_path, $sess_session_name;
       
  $sess_save_path = $save_path;
  $sess_session_name = $session_name;
  return(true);
}

function close() {
  return(true);
}

function read ($id) {
  global $sess_save_path, $sess_session_name;

  $sess_file = "$sess_save_path/sess_$id";
  if ($fp = @fopen($sess_file, "r")) {
    $sess_data = fread($fp, filesize($sess_file));
    return($sess_data);
  } else {
    return(""); // Deve retornar "" aqui.
  }

}

function write ($id, $sess_data) {
  global $sess_save_path, $sess_session_name;

  $sess_file = "$sess_save_path/sess_$id";
  if ($fp = @fopen($sess_file, "w")) {
    return(fwrite($fp, $sess_data));
  } else {
    return(false);
  }

}

function destroy ($id) {
  global $sess_save_path, $sess_session_name;
       
  $sess_file = "$sess_save_path/sess_$id";
  return(@unlink($sess_file));
}

/************************************************
 * AVISO - voc&ecirc; precisa implementar alguns tipos*
 *  de rotinas de garbage collection aqui.      *
 ************************************************/
function gc ($maxlifetime) {
  return true;
}

session_set_save_handler ("open", "close", "read", "write", "destroy", "gc");

session_start();

// Use o resto da sess&atilde;o normalmente

?>
]]>
      </programlisting>
     </example>
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

<?xml version="1.0" encoding="iso-8859-1"?>
<!-- EN-Revision: 1.14 Maintainer: fernandoc Status: ready -->
 <reference id="ref.mysql">
  <title>MySQL</title>
  <titleabbrev>MySQL</titleabbrev>

  <partintro>
   <section id="mysql.intro">
    &reftitle.intro;
    <para>
     Estas funções permitem acessar os servidores de banco de dados do MYSQL.
     Maiores informações podem ser encontradas em <ulink
     url="&url.mysql;">&url.mysql;</ulink>.
    </para>
    <para>
     Documentação para MySQL pode ser encontrada em <ulink
     url="&url.mysql.docs;">&url.mysql.docs;</ulink>.
    </para>
   </section>

   <section id="mysql.requirements">
    &reftitle.required;
    <para>
     Para ter estas funções disponiveis, você deve compilar o PHP com
     suporte a MySQL.
    </para>
   </section>

   <section id="mysql.installation">
    &reftitle.install;
    <para>
     Usando a opção de configuração <option role="configure">--with-mysql</option>
     você permite ao PHP acessar banco de dados do MySQL. Se você usar esta opção
     sem especificar o caminho para o MySQL, o PHP usará as bibliotecas do cliente
     que vem com ele. Com o PHP4, o suporte a MySQL esta sempre ativado; se você não especificar
     a opção de configuração, as bibliotecas que vem com o PHP serão usadas. Usuários que
     usam outras aplicações que usam MySQL (por exemplos, executando PHP3 e PHP4
     como modulos do apache, ou auth-mysql) devem sempre espicificar o
     caminho para o MySQL: <option
     role="configure">--with-mysql=/caminho/para/mysql</option>. Isto irá forçar o PHP
     a usar as bibliotecas de cliente instaladas com o MySQL, evitando quaisquer
     conflitos.
    </para>
    <warning>
     <para>
      Quebras e problemas de inicialização do <literal>PHP</literal> podem ser encontrados
      quando carrega-se esta extensão em conjunto com a extensão recode.
      Veja a extensão <link linkend="ref.recode">recode</link> para maiores
      informações.
     </para>
    </warning>
   </section>

    &reference.mysql.ini;

   <section id="mysql.resources">
   &reftitle.resources;
    <para>
     Há dois tipos de recursos usados no modulo MySql. O primeiro é a identificação
     do link da conexão com o banco de dados, o segundo é o que guarda o resultado
     de uma query.
    </para>
   </section>

   &reference.mysql.constants;


   <section id="mysql.examples">
   &reftitle.examples;
    <para>
     Este exemplo simples mostra como conectar, executar uma query, mostrar
     o resultado e desconectar do banco de dados do MySQL.
     <example>
      <title>Exemplo da extensão do MySQL</title>
      <programlisting role="php">
<![CDATA[
<?php
    /* Conectando, escolhendo o banco de dados */
    $link = mysql_connect("mysql_host", "mysql_user", "mysql_password")
        or die("Não pude conectar");
    print "Conexão bem sucedida";
    mysql_select_db("my_database") or die("Não pude selecionar o banco de dados");

    /* Fazendo a query SQL*/
    $query = "SELECT * FROM my_table";
    $result = mysql_query($query) or die("Query falhou");

    /* Mostrando os resultados em HTML */
    print "<table>\n";
    while ($line = mysql_fetch_array($result, MYSQL_ASSOC)) {
        print "\t<tr>\n";
        foreach ($line as $col_value) {
            print "\t\t<td>$col_value</td>\n";
        }
        print "\t</tr>\n";
    }
    print "</table>\n";

    /* Liberando o resultado */
    mysql_free_result($result);

    /* Fechando a conexão */
    mysql_close($link);
?>
]]>
      </programlisting>
     </example>
    </para>
   </section>
  </partintro>

&reference.mysql.functions;

 </reference>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->


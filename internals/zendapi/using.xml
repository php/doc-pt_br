<?xml version="1.0" encoding="ISO-8859-1" ?>
<!-- EN-Revision: 1.1 Maintainer: narigone Status: ready -->
 <sect1 xml:id="zend.using" xmlns="http://docbook.org/ns/docbook">
  <title>Usando Extensões</title>
  <para>
   Dependendo de processo de compilação que você escolheu, você deve ter no final ou
   um binário novo do PHP para ser montado com o seu servidor Web (ou executar como CGI), ou com um .so (objeto compartilhado). Se
   você compilou o arquivo de exemplo <filename>first_module.c</filename> como um objeto compartilhado, o arquivo resultante
   deve ser <filename>first_module.so</filename>. Para usá-lo, você tem que copiá-lo
   para um lugar onde ele seja acessível pelo PHP. Um procedimento simples de teste
   é copiá-lo para o seu diretório <filename>htdocs</filename> e testar com
   o código fonte em <xref linkend='example.testfile'/>.
   Se você compilou a sua extensão estaticamente no binário do PHP,
   omita a chamada à função <function>dl</function>, já que funcionalidade
   do módulo está disponível instantaneamente para seus scripts.
   <warning>
    <para>
     Por razões de segurança, você <emphasis>não deve</emphasis> colocar seus
     módulos dinâmicos em diretórios acessíveis publicamente. Embora seja <emphasis>possível</emphasis>
     e simplificar os testes, você deve colocá-los em um diretório separado
     em ambientes de produção.
    </para>
   </warning>
  </para>
  <example xml:id='example.testfile'>
   <title>Um arquivo de teste para first_module.so.</title>
   <programlisting role='php'>
<![CDATA[
<?php

// remove as barras de comentário se necessário
// dl("first_module.so");

$param = 2;
$return = first_module($param);

print("Nós enviamos '$param' e recebemos '$return'");

?>
]]>
   </programlisting>
  </example>
  <para>
   Chamar esse arquivo do PHP no seu servidor web deve mostrar a saída
   mostrada em  <xref linkend='fig.out-first'/>.
   <figure xml:id='fig.out-first'>
    <title>Saída de first_module.php.</title>
    <graphic fileref="figures/zend.02-first-module-output.png"/>
   </figure>
  </para>
  <para>
   Se necessário, o módulo dinâmico é carregado pela chamada da
   função <function>dl</function>. Essa função procura pelo
   objeto compartilhado especificado, carregá-o e torna suas funções
   disponíveis para o PHP. O módulo exporta a função
   <function>first_module</function>, que aceita um único
   parâmetro, converte-o para inteiro, e retorna o resultado
   da conversão.
  </para>
  <para>
   Se você chegou até aqui, parabéns! Você acabou de montar sua
   primeira extensão do PHP.
  </para>
 </sect1>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

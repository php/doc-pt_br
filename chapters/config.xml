<?xml version="1.0" encoding="iso-8859-1"?>
<!-- EN-Revision: 1.52 Maintainer: andre_ae Status: ready -->
 <chapter id="configuration">
  <title>Configuração</title>

  <sect1 id="configuration.file">
   <title>O arquivo de configuração</title>

   <simpara>
    O arquivo de configuração (chamado <filename>php3.ini</filename> no
    PHP 3.0, e simplesmente &php.ini; no PHP 4.0)
    é lido quando o PHP inicia. Para as versões de módulos de servidor do PHP,
    isso só acontece uma vez quando o servidor é iniciado. Para a versão
    <acronym>CGI</acronym> acontece em cada chamada.</simpara>
   <para>
    <example>
     <title>&php.ini; example</title>
     <programlisting role="ini">
<![CDATA[
; any text on a line after an unquoted semicolon (;) is ignored
[php] ; section markers (text within square brackets) are also ignored
; Boolean values can be set to either:
;    true, on, yes
; or false, off, no, none
register_globals = off
magic_quotes_gpc = yes

; you can enclose strings in double-quotes
include_path = ".:/usr/local/lib/php"

; backslashes are treated the same as any other character
include_path = ".;c:\php\lib"
]]>
</programlisting>
<!-- TODO: add more details about values and expressions -->
    </example>
   </para>

   <simpara>
    Usando o PHP como um módulo do Apache, você também pode mudar
    as definições de configuração usando diretivas na configuração dos arquivos
    Apache e dos arquivos .htaccess (Você irá precisar de privilégios
    "AllowOverride Options" ou "AllowOverride All")</simpara>

   <simpara>
    Com o PHP 3.0, existem diretivas Apache que correspondem a cada
    definição de configuração no nome <filename>php3.ini</filename>,
    exceto o nome predefinido por "php3_".</simpara>

   <para>
    Com o PHP 4.0, existem várias diretivas do Apache que lhe permitem
    mudar a configuração PHP dentro do arquivo de configuração do Apache.
    <variablelist>
     <varlistentry>
      <term>
       <systemitem role="directive">php_value</systemitem>
       <parameter>nome</parameter>
       <parameter>valor</parameter>
      </term>
      <listitem>
       <para>
        Isso define o valor da variável especificada.</para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>
       <systemitem role="directive">php_flag</systemitem>
       <parameter>nome</parameter>
       <parameter>on|off</parameter>
      </term>
      <listitem>
       <para>
        Isto é usado para definir uma opção de configuração Booleana.</para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>
       <systemitem role="directive">php_admin_value</systemitem>
       <parameter>nome</parameter>
       <parameter>valor</parameter>
      </term>
      <listitem>
       <para>
        Isto define o valor da variável especificada. Definições de
        configuração "Admin" só podem ser definidas a partir dos
        arquivos principais de configuração do Apache, e não
        dos arquivos .htaccess.</para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>
       <systemitem role="directive">php_admin_flag</systemitem>
       <parameter>nome</parameter>
       <parameter>on|off</parameter>
      </term>
      <listitem>
       <para>
        Isto é usado para definir uma opção de configuração Booleana.</para>
      </listitem>
     </varlistentry>
    </variablelist>
   </para>
   <para>
    <example>
     <title>Exemplo de configuração do Apache</title>
     <programlisting role="ini">
<![CDATA[
<IfModule mod_php4.c>
  php_value include_path ".:/usr/local/lib/php"
  php_flag safe_mode on
</IfModule>
<IfModule mod_php3.c>
  php3_include_path ".:/usr/local/lib/php"
  php3_safe_mode on
</IfModule>
]]>
</programlisting>
    </example>
   </para>

   <simpara>
    Você pode visualizar as definições dos valores de configuração na
    saída de <function>phpinfo</function>. Você também pode acessar
    os valores de definições de configuração originais usando
    <function>get_cfg_var</function>.
   </simpara>
   
   <sect2 id="ini.sect.general">
    <title>Diretivas Gerais de Configuração</title>
    <para>
     <variablelist>
      <varlistentry id="ini.allow-url-fopen">
       <term>
        <parameter>allow_url_fopen</parameter>
        <type>boolean</type>
       </term>
       <listitem>
        <para>
         Esta opção habilita o URL-aware fopen wrappers que possibilitam 
         o acesso a objetos URL
         como arquivos. Wrappers padrão são fornecidos para o acesso de
         <link linkend="features.remote-files">arquivos remotos</link>
         usando os protocolos FTP ou HTTP, algumas extensões como zlib podem 
         registrar wrappers adicionais.
        </para>

        <note>
         <para>
          Esta opção foi introduzida imediatamente após o lançamento da versão 
          4.0.3.
          Para versões superiores incluindo a 4.0.3, você só pode desabilitar essa
          função na
          compilação usando o argumento
          <link linkend="install.configure.disable-url-fopen-wrapper"><parameter>--disable-url-fopen-wrapper</parameter></link> na configuração (configure).
         </para>
        </note>

       </listitem>
      </varlistentry>

      <varlistentry id="ini.asp-tags">
       <term>
        <parameter>asp_tags</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <simpara>
         Habilita o uso de tags no estilo ASP &lt;% %&gt; em adição às tags
         &lt;?php ?&gt; usuais. Isso inclui a impressão resumida
         de valor de variável &lt;%= $valor %&gt;. Para maiores informações
         veja <link linkend="language.basic-syntax.phpmode">Saindo do HTML</link>.
        </simpara>

        <note>
         <para>O suporte para tags no estilo ASP foi adicionada na versão 3.0.4.</para>
        </note>

       </listitem>
      </varlistentry>

      <varlistentry id="ini.auto-append-file">
       <term>
        <parameter>auto_append_file</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
         Especifica o nome do arquivo que é automaticamente interpretado
         após o arquivo principal. O arquivo é incluído como se tivesse sido
         chamado com a função <function>include</function>, logo
         <link linkend="ini.include-path">include_path</link> é usado.</para>
        <para>
         O valor especial <systemitem class="constant">none</systemitem>
         desabilita o auto-appending.
         <note>
          <simpara>
           Se o script é finalizado com <function>exit</function>,
           auto-append <emphasis>não</emphasis> ocorrerá.</simpara>
         </note></para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.auto-prepend-file">
       <term>
        <parameter>auto_prepend_file</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
         Especifica o nome do arquivo que é automaticamente interpretado
         antes do arquivo principal. O arquivo é incluído como se tivesse sido
         chamado com a função <function>include</function>, logo
         <link linkend="ini.include-path">include_path</link> é usado.</para>
        <para>
         O valor especial <systemitem class="constant">none</systemitem>
         desabilita o auto-prepending.</para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.cgi-ext">
       <term>
        <parameter>cgi_ext</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.display-errors">
       <term>
        <parameter>display_errors</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
         Isso determina se os erros deve ser impressos na tela
         como parte da saída HTML ou não.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.doc-root">
       <term>
        <parameter>doc_root</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
         O "root directory" do PHP no servidor. Só é usado se não
         estiver vazio. Se o PHP é configurado com <link
         linkend="ini.safe-mode">safe mode</link>, nenhum
         arquivo acima desse diretório é acessível.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.engine">
       <term>
        <parameter>engine</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
         Essa diretiva só é realmente útil com a versão de módulo Apache
         do PHP. É usado por sites que queiram habilitar ou desabiliar
         a interpretação PHP num servidor por diretório ou
         por domínio virtual. Colocando <userinput>engine
         off</userinput> nos devidos lugares no arquivo
         <filename>httpd.conf</filename>, o PHP pode ser habilitado ou
         desabilitado.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.error-log">
       <term>
        <parameter>error_log</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
         Nome do arquivo onde erros de script devem ser guardados. Se o
         valor especial <literal>syslog</literal> for usado, os erros serão
         enviados para o logger do sistema. No UNIX, isto significa
         syslog(3) e no Windows NT isto significa o event log. O
         system logger não é suportado no Windows 95.</para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.error-reporting">
       <term>
        <parameter>error_reporting</parameter>
        <type>inteiro</type>
       </term>
       <listitem>
        <para>
         Define o nível de publicação de erros. O parâmetro é um inteiro
         representado um campo de bits. Adicione os valores que você
         queira do nível de publicação de erros.
         <table>
          <title>Níveis de Publicação de Erros</title>
          <tgroup cols="2">
           <thead>
            <row>
             <entry>valor do bit</entry>
             <entry>habilita publicação</entry>
            </row>
           </thead>
           <tbody>
            <row>
             <entry>1</entry>
             <entry>erros normais</entry>
            </row>
            <row>
             <entry>2</entry>
             <entry>avisos normais</entry>
            </row>
            <row>
             <entry>4</entry>
             <entry>erros de interpretação</entry>
            </row>
            <row>
             <entry>8</entry>
             <entry>avisos não críticos relacionados ao estilo</entry>
            </row>
           </tbody>
          </tgroup>
         </table>
         O valor padrão para esta diretiva é 7 (erros normais,
         avisos normais e erros de interpretação são mostrados).
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.html-errors">
       <term>
        <parameter>html_errors</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
         Desabilita tags HTML nas mensagens de erro.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.open-basedir">
       <term>
        <parameter>open_basedir</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
         Limita os arquios que podem ser abertos pelo PHP à arvore de diretórios
         especificada.
        </para>
        <para>
         Quando um script tenta abrir um arquivo com,
         por exemplo, fopen or gzopen, a localização desse arquivo é
         checada. Quando o arquivo está fora da árvore de diretórios especificada,
         o PHP irá se recusar a abrí-lo. Todos os links simbólicos são resolvidos,
         logo não é possível evitar essa restrição com um symlink.
        </para>
        <para>
         O valor especial <systemitem class="constant">.</systemitem>
         indica que o diretório onde o script está armazenado será
         usado como o diretório base.
        </para>
        <para>
         No Windows, separe os diretórios com ponto-e-vírgula. Em todos os outros
         sistemas, separe os diretórios com dois pontos. Como um módulo Apache,
         os caminhos de open_basedir de diretórios pais agora são
         automaticamente herdados.
        </para>
        <para>
          A restrição imposta com open_basedir é na verdade um prefixo, e
          não um nome de diretório. Isso significa que "open_basedir =
          /dir/incl" também permite acesso a "/dir/include" e "/dir/incls"
          se eles existem. Quando você deseja restringir o acesso somente
          ao diretório específico, o termine com uma barra. Por exemplo:
          "open_basedir = /dir/incl/"
        </para>
        <note>
         <para>O supporte para múltiplos diretórios foi adicionado na versão 3.0.7.</para>
        </note>
        <para>
         O padrão é permitir que todos os arquivos sejam abertos.
        </para>
       </listitem>
      </varlistentry>
      
      <varlistentry id="ini.gpc-order">
       <term>
        <parameter>gpc_order</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
         Define a ordem de interpretação da variável GET/POST/COOKIE . A
         definição padrão para essa diretiva é "GPC". Definindo-a para
         "GP", por exemplo, fará com que o PHP ignore completamente cookies
         e irá sobescrever quaisquer variáveis enviadas com o método GET por variáveis
         do método POST com o mesmo nome.
        </para>
        <para>
         Note que esta opção não está disponível no PHP 4.
         Use <link linkend="ini.variables-order">variables_order</link>
         ao invez.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.variables-order">
       <term>
        <parameter>variables_order</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
         Define a ordem de prioridade das variáveis EGPCS (Environment,
         GET, POST, Cookie, Server). A definição padrão para essa
         diretiva é "EGPCS". Definindo como "GP", por exemplo,
         fará com que o PHP ignore completamente as variáveis de ambiente
         (environment) cookies e server, e sobrescrever qualquer
         variavel do metodo GET com variaveis do modo POST com o mesmo nome.
        </para>
        <para>
         Veja também <link
linkend="ini.register-globals">register_globals</link>.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.ignore-user-abort">
       <term>
        <parameter>ignore_user_abort</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
         Habilitado (On) por padrão. Se desabilitado (Off) os scripts irão ser finalizados
         tão logo quanto ele tentar imprimir algo e descobrir que o usuário cancelou
         a conexão.
         Veja também <function>ignore_user_abort</function>.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.implicit-flush">
       <term>
        <parameter>implicit_flush</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
         &false; por default. Alterando para &true, diz ao PHP para chamar as
         rotinas de impressão/output automaticamente depois de cada comando de saída.
         Isto é equivalente a chamar a função
         <function>flush</function> depois de cada chamada a
         <function>print</function> ,<function>echo</function> ou depois de qualquer outro
         bloco <literal>HTML</literal>.
        </para>
        <para>
         Quanto utilizando o <literal>PHP</literal> em um ambiente Web, ativar
         esta opção tem sérias implicações na performance e é geralmente
         recomendado para propósitos de debug. Este valor é &true; por
         default quando operando sobre a <literal>CLI SAPI</literal>.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.include-path">
       <term>
        <parameter>include_path</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
         Especifica uma lista de diretórios onde as funções
         <function>require</function>, <function>include</function>
         e <function>fopen_with_path</function> procuram por
         arquivos. O formato é o mesmo da variável de ambiente <envar>PATH</envar>
         do sistema: uma lista de diretórios separados por
         dois pontos no UNIX ou ponto-e-vírgula no Windows.
         <example>
          <title>include_path no UNIX</title>
          <programlisting role="php3.ini">
<![CDATA[
include_path=.:/home/httpd/php-lib
]]>
          </programlisting>
         </example>
         <example>
          <title>include_path no Windows</title>
          <programlisting role="php3.ini">
<![CDATA[
include_path=".;c:\www\phplib"
]]>
          </programlisting>
         </example>
         O valor padrão para essa diretiva é <literal>.</literal>
         (apenas o diretório atual).</para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.isapi-ext">
       <term>
        <parameter>isapi_ext</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.log-errors">
       <term>
        <parameter>log_errors</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
         Diz se as mensagens de erro devem ser salvas no
         arquivo de log do servidor. Logo, esta opção é específica por servidor.</para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.magic-quotes-gpc">
       <term>
        <parameter>magic_quotes_gpc</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
         Define o estado de magic_quotes para as operações
         GPC (Get/Post/Cookie). Quando os magic_quotes estão habilitados (on),
         todas ' (aspas simples), &quot; (aspas duplas), \ (barra invetida) e NULL's são
         automaticamente prefixadas (escaped) com a barra invertida. Se magic_quotes_sybase
         também está habilitado (on), uma aspa simples é escapada como aspa simples
         ao invés da barra invertida.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.magic-quotes-runtime">
       <term>
        <parameter>magic_quotes_runtime</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
         Se <parameter>magic_quotes_runtime</parameter> é habilitado,
         a maioria das funções que retornam dados de qualquer tipo de fonte
         externa incluindo banco de dados e arquivos de texto terão aspas
         escapadas com barra invertida. Se <parameter>magic_quotes_sybase</parameter>
         também estiver habilitado (on), aspa simpes é escapada como aspa simples
         ao invés de barra invertida.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.magic-quotes-sybase">
       <term>
        <parameter>magic_quotes_sybase</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
         Se <parameter>magic_quotes_sybase</parameter> também estiver habilitado (on),
         aspas simples é escapada como aspa simples ao invés de barra invertida
         se <parameter>magic_quotes_gpc</parameter> ou
         <parameter>magic_quotes_runtime</parameter> estiverem habilitados.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.max-execution-time">
       <term>
        <parameter>max_execution_time</parameter>
        <type>inteiro</type>
       </term>
       <listitem>
        <para>
         Isto define a quantide máxima de tempo em segundos que um script é permitido
         rodar antes de ser finalizado pelo interpretador. Isto previne
         scripts mal escritos de travar o servidor. O 
         valor padrão é <literal>30</literal>.
        </para>
        <para>
         O tempo máximo de execução não é afetado por qualquer system calls,
         a função <function>sleep</function>, etc.  Veja a função
         <function>set_time_limit</function> para mais
         detalhes.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.memory-limit">
       <term>
        <parameter>memory_limit</parameter>
        <type>inteiro</type>
       </term>
       <listitem>
        <para>
         Isto define a quantidade máxima de memória em bytes que um script
         é capaz de alocar. Isto ajuda a prevenir scripts mal escritos de consumir
         toda a memória disponível no servidor.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.nsapi-ext">
       <term>
        <parameter>nsapi_ext</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.precision">
       <term>
        <parameter>precision</parameter>
        <type>inteiro</type>
       </term>
       <listitem>
        <simpara>
         O número de digitos significativos mostrado em números de ponto flutuante.
        </simpara>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.register-argc-argv">
       <term>
        <parameter>register_argc_argv</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <simpara>
         Diz ao PHP se declara as variaveis argv &amp; argc
         (que conteriam a informação do GET).
        </simpara>
        <simpara>
          Veja também <link linkend="features.commandline">PHP em linha de comando</link>.
          Esta diretiva se tornou disponível no PHP 4.0.0 e
          estava sempre habilitanda antes desta versão.
        </simpara>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.post_max_size">
       <term>
        <parameter>post_max_size</parameter>
        <type>integer</type>
       </term>
       <listitem>
        <simpara>
         Define o tamanho máximo dos dados informados via POST. Esse parâmetro afeta
         o upload de arquivos. Para poder receber arquivos grandes, este valor precisa ser maior
         que o <link linkend="upload-max-filesize">upload_max_filesize</link>.
        </simpara>
        <simpara>
         Se o limite de memória foi informado no script de configuração,  <link
         linkend="ini.memory-limit">memory_limit</link> afetará o
         upload de arquivos. Ou seja, o <link
         linkend="ini.memory-limit">memory_limit</link> também precisa
         maior que <link
         linkend="ini.post-max-size">post_max_size</link>.
        </simpara>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.register-globals">
       <term>
        <parameter>register_globals</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
         Diz quando registrar as variáveis EGPCS (Environment, GET,
         POST, Cookie, Server) como globais ou não. Você pode
         desabilitar isso se você não quer misturar o escopo global do seu script
         com dados de usuário. Isto faz mais senso
         quando usado juntamente com <link
         linkend="ini.track-vars">track_vars</link>. Neste caso
         você pode acessar todas as variáveis EGPCS através das matrizes
         <varname>$_ENV</varname>,
         <varname>$_GET</varname>,
         <varname>$_POST</varname>,
         <varname>$_COOKIE</varname> e
         <varname>$_SERVER</varname>
         no escopo global.
        </para>
        <para>
         Please note that you need to set AllowOveride All in your
         Directory block in the apache config file for this to work.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.short-open-tag">
       <term>
        <parameter>short_open_tag</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
         Diz quando a forma curta (<userinput>&lt;? ?&gt;</userinput> do tag
         de abertura do PHP deve ser permitida.  Se você quer usar PHP cobinado com
         XML, você deve desabilitar essa opção.  Se desabilitada, você deve usar
         a forma longa da tag de abertura
         (<userinput>&lt;?php ?&gt;</userinput>).</para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.sql.safe-mode">
       <term>
        <parameter>sql.safe_mode</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.track-errors">
       <term>
        <parameter>track_errors</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
         Se habilitado, a última mensagem de erro sempre estará presente
         na variável global <symbol>$php_errormsg</symbol>.</para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.track-vars">
       <term>
        <parameter>track_vars</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
         Se habilitado, então as variáveis Environment, GET, POST, Cookie,
         e Server poderão ser encontradas nas matrizes associativas globais
         <varname>$_ENV</varname>,
         <varname>$_GET</varname>,
         <varname>$_POST</varname>,
         <varname>$_COOKIE</varname> e
         <varname>$_SERVER</varname>.
        </para>
        <para>
         Note que no PHP 4.0.3, <systemitem
         role="directive">track_vars</systemitem> sempre está habilitada.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.upload-tmp-dir">
       <term>
        <parameter>upload_tmp_dir</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
         O diretório temporário usado para armazenar arquivos
         durante o upload de arquivos. Deve ter permissão de escrita para qualquer usuário em
         que o PHP esteja sendo rodado.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.upload-max-filesize">
       <term>
        <parameter>upload_max_filesize</parameter>
        <type>inteiro</type>
       </term>
       <listitem>
        <para>
         O tamanho máximo de um upload de arquivo. O valor é
         expresso em bytes.
        </para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.user-dir">
       <term>
        <parameter>user_dir</parameter>
        <type>string</type>
       </term>
       <listitem>
        <para>
         O caminho base do usado como diretório home do usuário
         para arquivos PHP, por exemplo
         <literal>public_html</literal>.</para>
       </listitem>
      </varlistentry>

      <varlistentry id="ini.warn-plus-overloading">
       <term>
        <parameter>warn_plus_overloading</parameter>
        <type>booleano</type>
       </term>
       <listitem>
        <para>
         Se habilitado, esta opção faz com que o PHP imprima um aviso quando o operador
         mais (<literal>+</literal>) é usado em variáveis string.
         Isto é para tornar mais fácil encontrar scripts que precisam ser reescritos
         para substituir pelo concatenador de strings
         (<literal>.</literal>).</para>
       </listitem>
      </varlistentry>
     </variablelist>
    </para>
   </sect2>

<!--
  multiple definitions of IDs

   <sect2 id="ini.sect.mail">
    <title>Diretivas de Configuração de Correio (Mail)</title>
    <variablelist>
    <varlistentry id="ini.smtp">
      <term>
       <parameter>SMTP</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        Nome DNS ou endereço IP do servidor SMTP para o envio de emails com a
        função <function>mail</function>
        </para>
      </listitem>
     </varlistentry>

     <varlistentry id="ini.sendmail-from">
      <term>
       <parameter>sendmail_from</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        Qual endereço "De:" deve ser usado no envio de emaila partir do
        PHP no Windows.</para>
      </listitem>
     </varlistentry>

     <varlistentry id="ini.sendmail-path">
      <term>
       <parameter>sendmail_path</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        Onde o programa <command>sendmail</command> pode ser encontrado,
        usualmente <filename>/usr/sbin/sendmail</filename> ou
        <filename>/usr/lib/sendmail</filename>
        <command>configure</command> faz uma tentativa de localizá-lo
        para você e definí-lo como padrão, mas se isso falhar,
        você pode definir isso aqui.</para>
       <para>
        Sistemas que não usam sendmail devem definir essa diretiva ao
        sendmail wrapper/replacement que seu sistema de mail oferece, se existir algum.
        Por exemplo, usuários <ulink url="&url.qmail;">Qmail</ulink>
        podem nromalmente definir a diretiva para
        <filename>/var/qmail/bin/sendmail</filename>.</para>
      </listitem>
     </varlistentry>
    </variablelist>
   </sect2>

-->

   <sect2 id="ini.sect.safe-mode">
    <title>Diretivas de Configuração de Modo Seguro (Safe Mode)</title>
    <variablelist>

     <varlistentry id="ini.safe-mode">
      <term>
       <parameter>safe_mode</parameter>
       <type>booleano</type>
      </term>
      <listitem>
       <para>
        Quando habilitar o modo seguro do PHP. Leia o <link linkend="security">Capítulo de Segurança</link> para maiores informações</para>
      </listitem>
     </varlistentry>

     <varlistentry id="ini.safe-mode-exec-dir">
      <term>
       <parameter>safe_mode_exec_dir</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        Se o PHP está sendo usado em modo seguro, <function>system</function> e
        as outras funções de execução do sistema recusam-se a iniciar programas que
        não estejam neste diretório.</para>
      </listitem>
     </varlistentry>

    </variablelist>
   </sect2>

   <sect2 id="ini.sect.debugger">
    <title>Diretivas ed Configuração do Debugger</title>
    <variablelist>

     <varlistentry id="ini.debugger.host">
      <term>
       <parameter>debugger.host</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        Nome do DNS ou endereço IP da máquina usada pelo debuger.</para>
      </listitem>
     </varlistentry>

     <varlistentry id="ini.debugger.port">
      <term>
       <parameter>debugger.port</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        Número da porta usada pelo debugger.</para>
      </listitem>
     </varlistentry>

     <varlistentry id="ini.debugger.enabled">
      <term>
       <parameter>debugger.enabled</parameter>
       <type>booleano</type>
      </term>
      <listitem>
       <para>
        Se o debugger está habilitado ou não.</para>
      </listitem>
     </varlistentry>

    </variablelist>
   </sect2>

   <sect2 id="ini.sect.extension">
    <title>Diretivas de Carregamento de Extensão</title>
    <variablelist>
     
     <varlistentry id="ini.enable-dl">
      <term>
        <parameter>enable_dl</parameter>
        <type>booleano</type>
      </term>
      <listitem>
        <para>
         Esta diretiva só é realmente útil na versão do módulo Apache
         do PHP. Você pode habilitar ou desabilitar o carregamento
         dinâmico de extensões PHP com <function>dl</function> em servidor
         por virtual ou por diretório.
        </para>
        <para>
         A principal razão em desabilitar o carregamento dinâmico é
         a segurança. Com carregamento dinâmico, é possível ignorar todas
         as restrições safe_mode e open_basedir.
        </para>
        <para>
         O padrão é permitir o carregamento dinâmico, exceto ao ser usado o
         modo seguro.  No modo seguro, é sempre impossível usar
         <function>dl</function>.
        </para>
      </listitem>
     </varlistentry>
      
     <varlistentry id="ini.extension-dir">
      <term>
       <parameter>extension_dir</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        Nesse diretório o PHP deve procurar por extensões dinamicamente
        carregáveis.</para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.extension">
      <term>
       <parameter>extension</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        Que extensões dinamicamente carregáveis devem ser carregadas quando o PHP inicia.
       </para>
      </listitem>
     </varlistentry>
     
    </variablelist>
   </sect2>

   <sect2 id="ini.sect.msql">
    <title>Diretivas de Configuração mSQL</title>
    <variablelist>
     
     <varlistentry id="ini.msql.allow-persistent">
      <term>
       <parameter>msql.allow_persistent</parameter>
       <type>booleano</type>
      </term>
      <listitem>
       <para>
        Permite ou não conexões persistentes mSQL.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.msql.max-persistent">
      <term>
       <parameter>msql.max_persistent</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        O número máximo de conexões persistentes mSQL por processo.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.msql.max-links">
      <term>
       <parameter>msql.max_links</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        O número máximo de conexões mSQL por processo, inclunido
        conexões persistentes.
       </para>
      </listitem>
     </varlistentry>
     
    </variablelist>
   </sect2>

   <sect2 id="ini.sect.pgsql">
    <title>Diretivas de Configuração Postgres</title>
    <variablelist>
     
     <varlistentry id="ini.pgsql.allow-persistent">
      <term>
       <parameter>pgsql.allow_persistent</parameter>
       <type>booleano</type>
      </term>
      <listitem>
       <para>
        Permite ou não conexões persistentes Postgres.       
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.pgsql.max-persistent">
      <term>
       <parameter>pgsql.max_persistent</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        O número máximo de conexões persistentes Postgres por
        processo.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.pgsql.max-links">
      <term>
       <parameter>pgsql.max_links</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        O número máximo de conexões Postgres por processo,
        incluindo conexões persistentes.
       </para>
      </listitem>
     </varlistentry>
     
    </variablelist>
   </sect2>


   <sect2 id="ini.sect.sesam">
    <title>Diretivas de Configuração SESAM</title>
    <variablelist>
     
     <varlistentry id="ini.sesam-oml">
      <term>
       <parameter>sesam_oml</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        Nome da biblioteca BS2000 PLAM contendo os módulos de driver SESAM
        carregáveis.  Exigido para usar funções SESAM. A biblioteca
        BS2000 PLAM deve estar definida ACCESS=READ,SHARE=YES
        porque ela deve ser lida pelo id de usuário do servidor Apache.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.sesam-configfile">
      <term>
       <parameter>sesam_configfile</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        Nome do arquivo de configuração da aplicação  SESAM.  Exigido ao usar
        funções SESAM.  O arquivo BS2000 deve ser lido pelo id de usuário
        do servidor apache.
       </para>
       <para>
        O arquivo de configuração da aplicação irá normalmente conter um
        arquivo de configuração como (veja o manual de
        referência SESAM):
        <informalexample>
         <programlisting role="bs2000">
CNF=B
NAM=K
NOTYPE</programlisting>
        </informalexample>
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.sesam-messagecatalog">
      <term>
       <parameter>sesam_messagecatalog</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        Nome do arquivo de catálogo de mensagem SESAM.  Na maioria dos casos, esta
        diretiva é desnecessária. Apenas se o arquivo de mensagem SESAM
        não estiver instalado na tabela de arquivo de mensagen do sistema BS2000,
        pode então ser definida por esta diretiva.
       </para>
       <para>
        O catálgo de mensagem deve ser definido ACCESS=READ,SHARE=YES porque ele deve
        ser lido pelo id de usuário do servidor apache.
       </para>
      </listitem>
     </varlistentry>
     
    </variablelist>
   </sect2>


   <sect2 id="ini.sect.sybase">
    <title>Diretivas de Configuração Sybase</title>
    <variablelist>
     
     <varlistentry id="ini.sybase.allow-persistent">
      <term>
       <parameter>sybase.allow_persistent</parameter>
       <type>booleano</type>
      </term>
      <listitem>
       <para>
        Permite ou não conexões persistentes Sybase.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.sybase.max-persistent">
      <term>
       <parameter>sybase.max_persistent</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        O número máximo de conexões persistentes Sybase por
        processo.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.sybase.max-links">
      <term>
       <parameter>sybase.max_links</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        O número máximo de conexões Sybase por processo,
        incluindo conexões persistentes.
       </para>
      </listitem>
     </varlistentry>
     
    </variablelist>
   </sect2>

   <sect2 id="ini.sect.sybct">
    <title>Diretivas de Configuração Sybase-CT</title>
    <variablelist>
     
     <varlistentry id="ini.sybct.allow-persistent">
      <term>
       <parameter>sybct.allow_persistent</parameter>
       <type>booleano</type>
      </term>
      <listitem>
       <para>
        Permite ou não conexões persistentes Sybase-CT.
        O padrão é permitir.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.sybct.max-persistent">
      <term>
       <parameter>sybct.max_persistent</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        O número máximo de conexões persistentes Sybase-CT por processo.
        O padrão é -1 que significa ilimitado.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.sybct.max-links">
      <term>
       <parameter>sybct.max_links</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        O número máximo de conexões Sybase-CT por processo,
        incluindo conexões persistentes.  O padrão é -1 que
        significa ilimitado.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.sybct.min-server-severity">
      <term>
       <parameter>sybct.min_server_severity</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        Mensagens do servidor muito maiores ou iguais a 
        sybct.min_server_severity serão publicadas como avisos.  Este
        valor também pode ser definido a partir de um script chamando
        <function>sybase_min_server_severity</function>.  O padrão
        é 10, que publica erros de informação muito maiores ou maiores.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.sybct.min-client-severity">
      <term>
       <parameter>sybct.min_client_severity</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        Mensagens de biblioteca de clientes muito maiores ou iguais a
        sybct.min_client_severity serã publicadas como avisos.  Este
        valor também pode ser definido a partir de um script chamando
        <function>sybase_min_client_severity</function>.  O padrão
        é 10 que efetivamente desabilita publicações.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.sybct.login-timeout">
      <term>
       <parameter>sybct.login_timeout</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        O máximo de tempo a esperar em segundos por uma tentativa de conexão
        obter sucesso antes de retornar uma falha. Note que se
        max_execution_time foi excedido quando uma tentativa de conexão
        passa do tempo, seu script será terminado antes que possa executar uma
        ação na falha. O padrão é um minuto.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.sybct.timeout">
      <term>
       <parameter>sybct.timeout</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        O máximo de tempo a esperar em segundos por uma select_db ou chamada
        obter sucesso antes de retornar uma falha. Note que se
        max_execution_time foi excedido quando uma tentativa de conexão
        passa do tempo, seu script será terminado antes que possa executar uma
        ação na falha. O padrão é sem limite.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.sybct.hostname">
      <term>
       <parameter>sybct.hostname</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        O nome da máquina a partir da qual você diz se conectar, para
        ser mostrado por sp_who.  O padrão é nenhum (none).
       </para>
      </listitem>
     </varlistentry>
     
    </variablelist>
   </sect2>

   <sect2 id="ini.sect.informix">
    <title>Diretivas de Configuração Informix</title>
    <variablelist>
     
     <varlistentry id="ini.ifx.allow-persistent">
      <term>
       <parameter>ifx.allow_persistent</parameter>
       <type>booleano</type>
      </term>
      <listitem>
       <para>
        Permite ou não conexões persistentes Informix.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.ifx.max-persistent">
      <term>
       <parameter>ifx.max_persistent</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        O número máximo de conexões persistentes Informix por
        processo.
       </para>
      </listitem>
     </varlistentry>
     
     <varlistentry id="ini.ifx.max-links">
      <term>
       <parameter>ifx.max_links</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        O número máximo de conexões Informix por processo, incluindo
        conexões persistentes.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="ini.ifx.default-host">
      <term>
       <parameter>ifx.default_host</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        A máquina padrão a ser conectada quando nenhuma máquina é especificada
        em <function>ifx_connect</function> ou 
        <function>ifx_pconnect</function>.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="ini.ifx.default-user">
      <term>
       <parameter>ifx.default_user</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        O id de usuário padrão a ser usado quando nenhum é especificado
        em <function>ifx_connect</function> ou 
        <function>ifx_pconnect</function>.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="ini.ifx.default-password">
      <term>
       <parameter>ifx.default_password</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        A senha padrão a ser usada quando nenhuma é especificada
        em <function>ifx_connect</function> ou 
        <function>ifx_pconnect</function>.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="ini.ifx.blobinfile">
      <term>
       <parameter>ifx.blobinfile</parameter>
       <type>booleano</type>
      </term>
      <listitem>
       <para>
        Defina como &true; (verdadeiro) se você quer retornar colunas blob
        num arquivo, falso se você as quer na memória. Você pode
        sobescrever a definição em tempo de execução
        com <function>ifx_blobinfile_mode</function>.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="ini.ifx.textasvarchar">
      <term>
       <parameter>ifx.textasvarchar</parameter>
       <type>booleano</type>
      </term>
      <listitem>
       <para>
        Defina como &true; (verdadeiro) se você quer retornar colunas TEXT
        como strings normais em declarações select,
        falso se você deseja usar parâmetros id blob. Você pode
        sobescrever essa definição em tempo de execução com
       <function>ifx_textasvarchar</function>.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="ini.ifx.byteasvarchar">
      <term>
       <parameter>ifx.byteasvarchar</parameter>
       <type>booleano</type>
      </term>
      <listitem>
       <para>
        Defina como &true; (verdadeiro) se você quer retornar colunas BYTE
        como strings normais em chamadas select,
        falso se você deseja usar parâmetros id blob. Você pode
        sobescrever essa definição em tempo de execução com
        <function>ifx_textasvarchar</function>.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="ini.ifx.charasvarchar">
      <term>
       <parameter>ifx.charasvarchar</parameter>
       <type>booleano</type>
      </term>
      <listitem>
       <para>
        Defina como verdadeiro se você deseja cortar espaços de trilha
        de colunas CHAR ao consultá-las.
       </para>
      </listitem>
     </varlistentry>

     <varlistentry id="ini.ifx.nullformat">
      <term>
       <parameter>ifx.nullformat</parameter>
       <type>booleano</type>
      </term>
      <listitem>
       <para>
        Defina como verdadeiro se você quer retornar colunas &null;
        como a string literal "&null;", falso se você que-las
        retornadas como uma string vazia "". Você pode
        sobescrever essa definição em tempo de execução com
        <function>ifx_nullformat</function>.
       </para>
      </listitem>
     </varlistentry>


    </variablelist>
   </sect2>

   <sect2 id="ini.sect.bcmath">
    <title>Diretivas de Configuração BC Math</title>
    <variablelist>
     
     <varlistentry id="ini.bcmath.scale">
      <term>
       <parameter>bcmath.scale</parameter>
       <type>inteiro</type>
      </term>
      <listitem>
       <para>
        Número de dígitos decimais para todas as funções bcmath.
       </para>
      </listitem>
     </varlistentry>
     
    </variablelist>
   </sect2>

   <sect2 id="ini.sect.browscap">
    <title>Diretivas de Configuração de Capacidade de Navegador</title>
    <variablelist>
     
     <varlistentry id="ini.browscap">
      <term>
       <parameter>browscap</parameter>
       <type>string</type>
      </term>
      <listitem>
       <para>
        Nome do arquivo de capacidade do navegador. Veja também
        <function>get_browser</function>.
       </para>
      </listitem>
     </varlistentry>
     
    </variablelist>
   </sect2>

  </sect1>

 </chapter>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
-->
